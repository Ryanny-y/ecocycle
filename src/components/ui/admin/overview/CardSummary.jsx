import React from "react";

const CardSummary = ({ title }) => {
  return (
    <div className="bg-red-200">
      <div id="top">
        <p>{title}</p>
      </div>

    </div>
  )
};

export default CardSummary;